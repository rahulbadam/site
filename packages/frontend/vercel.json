{
  "buildCommand": "npm install --prefix ../.. && npm run build --workspace=packages/shared && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --prefix ../.."
}